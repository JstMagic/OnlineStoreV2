{
  "name": "onlinestorev2-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "type-check": "tsc --noEmit && eslint .",
    "test": "vitest run --passWithNoTests",
    "verify": "npm install --no-audit --no-fund --include=dev --loglevel=error && tsc --noEmit && eslint . && vitest run --passWithNoTests && (npm run build || tsc --noEmit)",
    "lint": "eslint ."
  },
  "dependencies": {
    "next": "^14.2.25",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/node": "^20.14.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "tailwindcss": "^3.4.10",
    "postcss": "^8.4.41",
    "autoprefixer": "^10.4.20",
    "vitest": "^2.0.5",
    "eslint": "^9.9.0",
    "@typescript-eslint/parser": "^8.3.0",
    "@typescript-eslint/eslint-plugin": "^8.3.0"
  }
}
